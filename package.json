{
  "name": "@tenjojeremy/web-toolkit",
  "version": "4.125.0",
  "repository": "git://github.com/tenjojeremy/Web-Toolkit.git",
  "description": "Library of reusable UI and utilities",
  "publisher": "tenjojeremy",
  "license": "MIT",
  "files": [
    "build"
  ],
  "scripts": {
    "start": "start-storybook -p 9002 -c .storybook -s public --quiet",
    "deploy": "yarn lint:check && yarn cy:run-ci && yarn build:storybook && firebase deploy && yarn build:library && yarn publish --minor && rmdir /s /q build && gpush",
    "build:snippets": "node tenjo-web-toolkit-snippets/snippets.gen.js",
    "build:library": "cross-env NODE_ENV=production BABEL_ENV=es babel --config-file ./.babelrc ./src --out-dir ./build/ --ignore 'src/**/*.spec.js','src/**/*.spec-cy.js','src/**/*.test.js','src/**/*.stories.js' ",
    "build:storybook": "build-storybook -c .storybook -o .storybook/storybook-build -s public",
    "deploy:firebase": "firebase deploy --token=$FIREBASE_DEPLOY_TOKEN",
    "deploy:quick": "yarn build:library && yarn publish --minor && rmdir /s /q build && gpush",
    "publish:snippets": "yarn build:snippets && cd tenjo-web-toolkit-snippets && vsce publish minor && git add -A && git commit -a --allow-empty-message && git push",
    "test": "jest",
    "pretty": "prettier \"**/*.js\" --write",
    "cy:open": "cypress open",
    "cy:run": "cypress run --browser chrome",
    "cy:run-ci": "start-server-and-test start http://localhost:9002 cy:run",
    "lint:fix": "eslint ./src --fix",
    "lint:check": "eslint ./src",
    "check-staged": "lint-staged"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,jsx}": [
      "yarn lint:fix",
      "yarn pretty",
      "prettier --list-different",
      "jest --passWithNoTests"
    ]
  },
  "eslintConfig": {
    "extends": "@tenjojeremy/eslint-config"
  },
  "prettier": "@tenjojeremy/prettier-config",
  "dependencies": {
    "@material-ui/core": "^4.3.2",
    "decamelize": "^3.2.0",
    "react-bottom-scroll-listener": "^3.0.0",
    "react-lazyload": "^2.6.2",
    "react-router-dom": "^5.1.2",
    "react-stars": "^2.2.5",
    "styled-components": "^4.4.0",
    "twit": "^2.2.11"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.7.2",
    "@storybook/addon-actions": "^5.1.11",
    "@storybook/addon-docs": "^5.2.3",
    "@storybook/react": "^5.2.8",
    "@tenjojeremy/babel-config": "^1.5.0",
    "@tenjojeremy/eslint-config": "^1.7.0",
    "@tenjojeremy/prettier-config": "^0.4.0",
    "cypress": "^3.5.0",
    "date-fns": "^2.3.0",
    "eslint-plugin-cypress": "^2.6.1",
    "firebase": "^7.0.0",
    "firebase-tools": "^7.2.2",
    "husky": "^3.0.3",
    "image-compressor.js": "^1.1.4",
    "jest": "^24.9.0",
    "lint-staged": "^9.5.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "start-server-and-test": "^1.10.0",
    "storybook-react-router": "^1.0.5",
    "yup": "^0.27.0"
  }
}
