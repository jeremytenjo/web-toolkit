export const wrapper = {
  position: 'relative',
}
export const leftArrow = {
  position: 'absolute',
  top: 0,
  bottom: 0,
  left: 0,
}

export const rightArrow = {
  position: 'absolute',
  top: 0,
  bottom: 0,
  right: 0,
  transform: 'rotate(180deg)',
}
